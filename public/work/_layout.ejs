<% if (environment == "development") { 
  var env_set = ""
} else {
  var env_set = root
} %>

<%- partial('../_templates/head', {env_root: env_set}) %>
<body>
  <%- partial('../_templates/header-title', {env_root: env_set}) %>
    <section id="main" class="main main-case-study">
    <div class="narrow">
        <ul class="tag-list">
            <% for(var tag in tags){ %>
                <li><%= tags[tag] %></li>
            <% } %>
        </ul>
    </div>
        <%- yield %>
    </section>
  <%- partial('../_templates/footer', {env_root: env_set}) %>
  <%- partial('../_templates/foot', {env_root: env_set}) %>
</body>
</html>